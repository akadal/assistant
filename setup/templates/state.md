# State — right now

<!-- Volatile context; ≤200 tokens (800 characters, enforced by lint).
     Only consolidation (and the human) updates this file.
     Use the fact grammar from memory/rules/format.md:
       - [YYYY-MM-DD→] something ongoing → memory/domains/<d>/<file>.md
       - [YYYY-MM-DD]  something that happened on that day
     Lines here are pointers, not storage. Every one of them should end in a path. -->

(nothing yet — this fills in after the first consolidation)
