---
type: index
updated: {{YYYY-MM-DD}}
---
# People and organisations — index

The alias table. Consolidation reads this file **in full** on every run: this is where namesakes
get caught, so it has to stay small enough to be cheap and complete enough to be trusted.

Format, one line each: `name — aliases — domains — path`

## Files

(nobody yet)

<!-- TEMPLATE NOTES (delete when you write the real file)
     - Example line:
         - Alex Rivera — "Alex", "A. Rivera" — [work, family] — memory/people/alex-rivera.md
     - An alias may appear in exactly one file across the whole repository. The same alias in two
       files is a lint ERROR, not a warning: a wrong merge is the most expensive mistake this
       system can make, and lint cannot spot it afterwards — everything still looks structurally valid.
     - Namesakes get a distinguishing suffix in the file name (alex-rivera-neighbour vs
       alex-rivera-colleague); the real spelling lives in `name`.
     - Budget: 2,000 characters, ceiling. When it no longer fits, that is a signal to check whether
       rarely-touched entities should be marked `access: rare`. -->
