---
type: log
date: {{YYYY-MM-DD}}
machine: {{machine}}
---
# {{YYYY-MM-DD}} ({{machine}})

## {{HH:MM}}

{{What came up, in plain prose, in the user's language.}}

<!-- TEMPLATE NOTES (delete when you write the real file)
     - Append-only. Add a new `## HH:MM` block; never rewrite an earlier one.
     - There is no classification field, and that is deliberate: guessing the domain, the type or
       the entity at capture time adds friction, and friction kills capture. Sorting is
       consolidation's job.
     - Long raw material (a transcript, a document) does not go here. It goes to
       memory/archive/<domain>/YYYY/, and the inbox gets a one-line summary plus the path.
     - No budget: this is the raw layer. -->
