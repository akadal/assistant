---
type: index
domain: {{domain}}
updated: {{YYYY-MM-DD}}
---
# {{Domain}} — index

Scope: {{one line — what belongs in this domain and what does not}}.

## Hot

(nothing yet)

## Files

(nothing yet)

<!-- TEMPLATE NOTES (delete when you write the real file)
     - ## Hot: at most 5 curated lines, maintained by consolidation. Each line is a fact in the
       grammar plus a full path, so that a common question is answered without opening a file.
     - ## Files: mechanically reproducible. One line per `status: active` file:
         - memory/domains/<d>/<file>.md — <summary> (active)
       Closed files collapse into a single count line: `closed: 3 files`.
     - Full paths, never [[wikilinks]] — resolving a wikilink costs an extra tool call.
     - Budget: 2,000 characters, ceiling. -->
