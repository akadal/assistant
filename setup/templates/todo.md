# todo.md — a deliberately minimal list

<!-- RULES:
- This is NOT a task manager: no due dates, no reminders, no priority scores. That scope gate is
  closed on purpose (docs/DESIGN.md §1.3). If you need alarms, use a calendar.
- Line grammar: `- [ ] open item` / `- [x] done`. Related records go at the end of the line as a
  wikilink, e.g. [[project-slug]]; lint checks that the target exists.
- Who writes: human and assistant, freely. Consolidation drops completed [x] items (git keeps the record).
- Cap: 20 open items (lint warns). Over the cap means it is time to prune, not time to raise the cap.
-->

(nothing yet)
