# CLAUDE.md

The operating rules for this repository live in [`AGENTS.md`](AGENTS.md). Read that file.

This one exists so that harnesses which look for `CLAUDE.md` find their way there. It is kept
deliberately empty of rules — two files of instructions would drift apart, and `AGENTS.md` is the
one that every other agent reads too.
